//lightspeed
#define c 299792458 
void sphere(int radius,float theta,double fai){    
    char radius, theta, fai;
    typedef X ; radius * sin(theta) * cos(fai);
    typedef Y ; radius * sin(theta) * sin(fai);
    typedef Z ; radius * cos(theta) ;
    this.radius = sqrt(X^2 + Y^2 + Z^2);
 }

void loop(){
    char a,b,c,d,e,f;
   for(a=0;a<b;a++)
   for(b=00;b<c;b++)
   for(c=000;c<d;c++)
   for(d=000;d>e;--d)
   for(e=00;e>f;--e)
   for(f=0;f>a;--f)
;}

void sphere(){
     int X,Y,Z,T,r;
     char theta,fai,time;
     const time = T;
     X = r * sin(theta) * cos(fai);
     Y = r * sin(theta) * sin(fai);
     Z = r * cos(theta);
     r ^ 2 = x ^ 2 + y ^ 2 + z ^ 2;

    const Length = 1; 
    const r = Length;
    Coordinate(0,0,1),//A
    Coordinate(acos(-1/3), 0,0), // B
    Coordinate(0, -acos(-1/3),0), // C
    Coordinate(0,0,acos(-1/3)) // D

;}
